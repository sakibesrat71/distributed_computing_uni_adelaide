public class GETClient {
    public static void main(String[] args) {
        // Parse server address/port (and optional station ID)
        // Send GET request with Lamport clock
        // Display JSON weather data (attribute: value)
    }
}
